<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projekt.mobilne.unipu.dnevnikkrvnogtlaka" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UnosActivity"
            android:label="@string/title_activity_unos" >
        </activity>
        <activity
            android:name=".PregledActivity"
            android:label="@string/title_activity_pregled" >
        </activity>
        <activity
            android:name=".PovijestActivity"
            android:label="@string/title_activity_povijest" >
        </activity>
    </application>

</manifest>
